const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('https://manuales-buffet.pages.dev/?v=15#/3/en', {waitUntil: 'networkidle0'}); const el = await page.$('h2 span'); const color = await page.evaluate(e => window.getComputedStyle(e).color, el); console.log('Color: ', color); await page.screenshot({path: 'test.png'}); await browser.close(); })();
